---
title: "Quick Start"
description: "Get Asta running in 5 minutes"
---

Get up and running with Asta in just a few steps.

## Prerequisites

- **Python 3.12 or 3.13** (3.14 not yet supported)
- **Git** for cloning the repository
- **Node 18+** and **Rust** (optional, for desktop app)

## Installation

<Steps>
<Step title="Clone the repository">
Clone Asta from GitHub:

```bash
git clone https://github.com/helloworldxdwastaken/asta.git
cd asta
```
</Step>

<Step title="Configure environment">
Copy the example environment file:

```bash
cp .env.example backend/.env
```

Add your API keys to `backend/.env` (optional - you can add them later in Settings).
</Step>

<Step title="Start Asta">
Use the control script to start the backend:

```bash
./asta.sh start
```

The script will automatically detect Python 3.12 or 3.13, create a virtual environment, install dependencies, and start the backend on port 8010.
</Step>

<Step title="Access the API">
Open your browser to:
- API Documentation: http://localhost:8010/docs
- Health Check: http://localhost:8010/health
</Step>
</Steps>

## Next Steps

<CardGroup cols={2}>
  <Card title="Desktop App" icon="window" href="/desktop/overview">
    Install the cross-platform desktop app
  </Card>
  <Card title="Configure Providers" icon="brain" href="/concepts/ai-providers">
    Set up Claude, OpenAI, or other AI providers
  </Card>
  <Card title="Telegram Bot" icon="paper-plane" href="/guides/telegram-bot">
    Connect your Telegram bot
  </Card>
  <Card title="Create Skills" icon="wand-magic-sparkles" href="/guides/creating-skills">
    Build custom workspace skills
  </Card>
</CardGroup>

## Control Script Commands

The `asta.sh` script provides several commands:

| Command | Description |
|---------|-------------|
| `./asta.sh start` | Start the backend |
| `./asta.sh stop` | Stop all services |
| `./asta.sh restart` | Restart the backend |
| `./asta.sh status` | Show service status |
| `./asta.sh doc` | Run diagnostics |
| `./asta.sh doc --fix` | Auto-fix common issues |
| `./asta.sh install` | Add `asta` to your PATH |

## Troubleshooting

<AccordionGroup>
<Accordion title="Python 3.14 not supported">
Asta requires Python 3.12 or 3.13. Python 3.14 is not yet compatible with pydantic and ChromaDB.

Install Python 3.12:
```bash
brew install python@3.12
```
</Accordion>

<Accordion title="Port 8010 already in use">
The script automatically frees port 8010. If it persists, run:

```bash
./asta.sh stop
./asta.sh start
```
</Accordion>

<Accordion title="Backend won't start">
Check the logs at `backend.log` and run diagnostics:

```bash
./asta.sh doc --fix
```
</Accordion>
</AccordionGroup>

## What's Next?

- [Full Installation Guide](/installation) - Detailed setup for all platforms
- [Architecture Overview](/concepts/architecture) - Understand how Asta works
- [API Reference](/api/authentication) - Explore the REST API
